// OPCODES

module.exports = [
  BR,
  ADD,
  LD,
  ST,
  JSR,
  ADD,
  LDR,
  STR,
  RTI,
  NOT,
  LDI,
  STI,
  JMP,
  null,
  LEA,
  TRAP
]

// Operate (ADD, AND, NOT)

// Data Movement (LD, LDI, LDR, LEA, ST, STR, STI)

// Control (BR, JSR, JMP, RTI, TRAP) 
